---
interface Props {
  compact?: boolean;
}

const { compact = false } = Astro.props;
---

<a href="/" class:list={['inline-flex items-center gap-2 no-underline text-[var(--ink)]', compact ? '' : '']}>
  <span class="flex h-8 w-8 items-center justify-center rounded-full bg-[var(--accent)] text-sm font-semibold text-white" aria-hidden="true">t</span>
  <span class="text-[1.35rem] font-bold tracking-tight">techlist</span>
  <span class="sr-only">techlist home</span>
</a>
